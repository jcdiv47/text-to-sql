/**
 * Placeholder title for a thread with no first user message yet. Must stay in
 * sync with `DEFAULT_TITLE` in `convex/threads.ts`, since both the client (reuse
 * an empty thread) and the server (auto-title on first message) compare against
 * it.
 */
export const DEFAULT_THREAD_TITLE = "新对话";
